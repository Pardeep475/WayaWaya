import 'package:flutter/material.dart';

class ChildExpandableModel {
  String date;
  String description;
  String points;
  IconData iconData;

  ChildExpandableModel(
      {this.date, this.description, this.points, this.iconData});
}
